# Dataset schemas

Machine-readable schemas for Gapwise Data belong here. Canonical data should converge on language-neutral JSON/GeoJSON plus schemas; TypeScript adapters are consumer conveniences rather than the long-term source representation.
