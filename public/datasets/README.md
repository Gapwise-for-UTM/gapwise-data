# Gapwise Data distribution

Canonical source files live under `data/utm`. Production builds publish the complete validated tree at `/datasets/utm/latest/` with a generated SHA-256 manifest. Consumers that need stable application behavior should prefer `api.gapwise.ca` or the official SDKs; raw dataset consumers can use these first-party URLs.
