# Gapwise platform redistribution

This branch moves public data distribution and data-production ownership toward `gapwise-data`, while keeping runtime consumers pinned to tested snapshots. It also coordinates with `gapwise-docs` and `gapwise` so public knowledge, data, and runtime behavior have distinct owners.
